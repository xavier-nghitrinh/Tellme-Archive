name: Update Folder List

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  update-list:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Generate folders.json and Update index.html
        run: |
          echo "[]" > folders.json
          # Loop through each date folder
          for folder_path in [0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]*/; do
            folder="${folder_path%/}"
            
            # Try 1: Extract thumbnail from index.html (matches TellMe export format)
            thumb=$(grep -oP '"thumbnail":\s*"\K[^"]+' "$folder/index.html" | head -1 || echo "")
            
            # Try 2: Fallback to any JPG if no thumbnail found in file metadata
            if [ -z "$thumb" ]; then
              thumb=$(find "$folder" -name "*.jpg" -o -name "*.png" | head -1 | sed "s|$folder/||" || echo "")
            fi
            
            # Add to json using jq
            jq --arg name "$folder" --arg thumb "$thumb" '. += [{name: $name, thumbnail: $thumb}]' folders.json > tmp.json && mv tmp.json folders.json
          done

          # Embed folders.json content back into index.html to update the homepage
          json_content=$(cat folders.json)
          # Use a temporary file to handle the replacement safely
          perl -0777 -pi -e 's/<script id="folder-data" type="application/json">.*?<\/script>/<script id="folder-data" type="application/json">\n        '"$json_content"'\n    <\/script>/s' index.html
          
      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add folders.json index.html
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update folders list and homepage" && git push)
